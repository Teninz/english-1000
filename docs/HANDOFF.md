# ShadowFox Eng — рабочая передача

Обновлено 21 сентября 2026. Актуальная спецификация — `docs/SRS.md`, программа словаря — `docs/OXFORD_PROGRAM.md`.

## Текущее состояние

| Раздел | Состояние |
|---|---|
| Версия | `1.0.4.0`, Android `versionCode 24` |
| Ветка | `fox-reference-chain` |
| Основная программа | Oxford 3000: 3810 карточек «слово + часть речи» уровней A1–B2; дополнительный словарь — 482 слова прежнего курса |
| Мотивация | «Путь Oxford 3000»: закреплённые слова, ближайшая веха, прогресс A1–B2, ритм 4/7 и достижения за устойчивые знания |
| Главный сценарий | «Занятие на сегодня»: повторения → новые слова → разбор ошибок, нагрузка меняется по очереди и точности |
| Android | Capacitor 8, минимальная поддерживаемая версия Android 8.0; Android 7 не поддерживается |
| Сборка | `.github/workflows/android.yml`; подписанные APK и AAB собираются из секретов GitHub |
| Магазин | RuStore, материалы в `store/`, скриншоты создаёт `node tools/store-screens.js` |

## Проверка и сборка

```text
npm ci
npm test
npm run build:web
npx cap sync android
cd android
./gradlew assembleRelease bundleRelease
```

Для выпуска поднять `versionCode` и `versionName` в `android/app/build.gradle`, `APP_VERSION` и верхнюю запись `CHANGELOG`
в `about.js`, версию `package.json` и имя кэша в `sw.js`. Workflow проверяет тесты, собирает подписанные APK/AAB,
проверяет подпись APK и публикует оба файла как артефакт `ShadowFoxEng-<версия>`.

## Что проверить на телефоне

1. Обновление поверх 1.0.3.7b и сохранность прогресса.
2. Стартовую проверку A2–B2 и миграцию прежних ключей прогресса.
3. «Занятие на сегодня» и обновление карточки «Путь Oxford 3000».
4. Фоновый режим «В дороге»: погашенный экран, системный плеер, гарнитура и возврат в приложение.
5. Тематический блиц: сворачивание, принудительное закрытие, таймер и системную кнопку «Назад».
6. Виджет слова, напоминания, экспорт и импорт резервной копии.

## Ближайшая продуктовая работа

1. Вычитать переводы и примеры A2–B2 блоками, после каждого уровня запускать `program-check` и тесты.
2. Проверить мотивационный экран при пустом, частичном и полном прогрессе на узком телефоне.
3. Обновить карточку RuStore под Oxford 3000 и заменить скриншоты перед публикацией 1.0.4.0.
4. Проверить подсказки по оптимизации батареи на Xiaomi, Huawei и Samsung.
